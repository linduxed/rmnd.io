en:
  date:
    formats:
      default:
        "%m/%d/%Y"
      with_weekday:
        "%a %m/%d/%y"

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"

  titles:
    application: Rmnd

  simple_form:
    labels:
      reminder:
        title: Title
        due_at: Due date
        description: Description
        repeat_frequency: Repeats

  helpers:
    submit:
      reminder:
        create: Add

  reminders:
    repeat_frequencies:
      daily: Daily
      weekly: Weekly
      monthly: Monthly
      yearly: Yearly
    reminder:
      repeats: Repeats %{frequency}
