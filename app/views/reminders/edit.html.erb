<h2><%= t(".title") %></h2>
<%= simple_form_for @reminder_form, url: reminder_path(@reminder_form.reminder) do |f| %>
  <%= render "form_fields", f: f %>
  or <%= link_to t(".go_back"), reminders_path %>
<% end %>
